<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restaurant Booking</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950 text-zinc-200 min-h-screen flex items-center justify-center">
  <div class="bg-zinc-900 rounded-lg shadow-lg w-full max-w-md p-6">
    <h1 class="text-3xl font-semibold text-center mb-6 text-zinc-100">Book Your Table</h1>
    <form action="/book" method="POST" class="space-y-4">
      
      <div>
        <label for="date" class="block text-sm font-medium text-zinc-300 mb-1">Date</label>
        <input type="date" id="date" name="date" required 
          class="w-full bg-zinc-800 border border-zinc-700 text-zinc-200 rounded-lg py-2 px-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      
      <div>
        <label for="timeFrom" class="block text-sm font-medium text-zinc-300 mb-1">From Time</label>
        <input type="time" id="timeFrom" name="timeFrom" required 
          class="w-full bg-zinc-800 border border-zinc-700 text-zinc-200 rounded-lg py-2 px-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      
      <div>
        <label for="timeTo" class="block text-sm font-medium text-zinc-300 mb-1">To Time</label>
        <input type="time" id="timeTo" name="timeTo" required 
          class="w-full bg-zinc-800 border border-zinc-700 text-zinc-200 rounded-lg py-2 px-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      
      <div>
        <label for="guests" class="block text-sm font-medium text-zinc-300 mb-1">Number of Guests</label>
        <input type="number" id="guests" name="guests" min="1" required 
          class="w-full bg-zinc-800 border border-zinc-700 text-zinc-200 rounded-lg py-2 px-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      <div>
        <label for="name" class="block  text-sm font-medium text-zinc-300 mb-1">Name</label>
        <input type="text" id="name" value="<%= user.username %>" name="name" required 
          class="w-full bg-zinc-800 border border-zinc-700 text-zinc-200 rounded-lg py-2 px-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      
      <div>
        <label for="contact" class="block text-sm font-medium text-zinc-300 mb-1">Contact</label>
        <input type="number" id="contact" name="contact" required 
          class="w-full bg-zinc-800 border border-zinc-700 text-zinc-200 rounded-lg py-2 px-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      
      <div>
        <button type="submit" 
          class="w-full bg-blue-700 hover:bg-blue-800 text-white font-medium py-2 px-4 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400">
          Check Availability
        </button>
      </div>
    </form>
  </div>

  <script>
    // Get today's date in YYYY-MM-DD format
    const today = new Date().toISOString().split('T')[0];
    // Set the min attribute of the date input
    document.getElementById('date').setAttribute('min', today);
  </script>
</body>
</html>
